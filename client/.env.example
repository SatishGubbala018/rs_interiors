# Base URL of your deployed backend (Render).
# This value is baked into the frontend bundle at build time.
# Set the same value in Vercel under Environment Variables as VITE_API_URL.
VITE_API_URL=https://your-backend-service.onrender.com

